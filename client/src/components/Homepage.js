import React from 'react'

const Homepage = ({ loggedIn }) => {
    if (loggedIn === 'true') {
        return (
            <></>
        )
    } else {
        return (
            <></>
        ) 
    }
}

export default Homepage
